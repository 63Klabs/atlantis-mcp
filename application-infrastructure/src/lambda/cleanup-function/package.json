{
  "name": "atlantis-mcp-server-cleanup-lambda",
  "version": "0.0.4",
  "description": "Cleanup Lambda function for Atlantis MCP Server - Removes orphaned Cognito accounts on DynamoDB TTL deletions",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "keywords": [
    "atlantis",
    "mcp",
    "cleanup",
    "cognito",
    "dynamodb-streams",
    "serverless"
  ],
  "author": "63Klabs",
  "license": "MIT",
  "devDependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1000.0",
    "@aws-sdk/client-ssm": "^3.1000.0",
    "aws-sdk-client-mock": "^4.1.0",
    "eslint": "^10.0.2",
    "fast-check": "^3.0.0",
    "jest": "^30.2.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
